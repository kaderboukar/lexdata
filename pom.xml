<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <groupId>com.lexdata</groupId>
    <artifactId>lexdata-backend</artifactId>
    <version>1.0.0-SNAPSHOT</version>
    <packaging>pom</packaging>
    <name>lexdata-backend</name>
    <description>Projet racine pour l'architecture microservices LEXDATA</description>

    <!-- Spring Boot parent corrigé -->
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.3.3</version>
        <relativePath />
    </parent>

    <properties>
        <java.version>17</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <spring-cloud.version>2023.0.1</spring-cloud.version>
    </properties>

    <modules>
        <module>lexdata-discovery-service</module>
        <module>lexdata-gateway</module>
        <module>lexdata-auth-service</module>
        <module>lexdata-user-service</module>
        <module>lexdata-juridique-base</module>
        <module>lexdata-veille</module>
        <module>lexdata-synthese</module>
        <module>lexdata-annuaire</module>
        <module>lexdata-tribune</module>
        <module>lexdata-contrats</module>
        <module>lexdata-calendrier</module>
        <module>lexdata-consultations</module>
        <module>lexdata-paiements</module>
        <module>lexdata-notifications</module>
        <module>lexdata-admin</module>
        <module>lexdata-monitoring</module>
    </modules>

    <dependencyManagement>
        <dependencies>
            <!-- Spring Cloud release train -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-dependencies</artifactId>
                <version>${spring-cloud.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>7.4</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <!-- Maven Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>17</release>
                </configuration>
            </plugin>

            <!-- Spring Boot Maven Plugin -->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>