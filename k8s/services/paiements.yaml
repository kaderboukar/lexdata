---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexdata-paiements
  namespace: lexdata
  labels:
    app: lexdata-paiements
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lexdata-paiements
  template:
    metadata:
      labels:
        app: lexdata-paiements
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: paiements
          image: lexdata/paiements:1.0.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 9012
          env:
            - name: SERVER_PORT
              value: "9012"
            - name: EUREKA_INSTANCE_HOSTNAME
              value: lexdata-paiements
          envFrom:
            - configMapRef:
                name: lexdata-config
            - secretRef:
                name: lexdata-secret
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 9012
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9012
            initialDelaySeconds: 60
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9012
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "384Mi"
              cpu: "200m"
            limits:
              memory: "768Mi"
              cpu: "750m"
---
apiVersion: v1
kind: Service
metadata:
  name: lexdata-paiements
  namespace: lexdata
spec:
  selector:
    app: lexdata-paiements
  ports:
    - port: 9012
      targetPort: 9012
