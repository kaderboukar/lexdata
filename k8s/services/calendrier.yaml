---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexdata-calendrier
  namespace: lexdata
  labels:
    app: lexdata-calendrier
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lexdata-calendrier
  template:
    metadata:
      labels:
        app: lexdata-calendrier
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: calendrier
          image: lexdata/calendrier:1.0.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 9010
          env:
            - name: SERVER_PORT
              value: "9010"
            - name: EUREKA_INSTANCE_HOSTNAME
              value: lexdata-calendrier
          envFrom:
            - configMapRef:
                name: lexdata-config
            - secretRef:
                name: lexdata-secret
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 9010
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9010
            initialDelaySeconds: 60
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9010
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "384Mi"
              cpu: "200m"
            limits:
              memory: "768Mi"
              cpu: "750m"
---
apiVersion: v1
kind: Service
metadata:
  name: lexdata-calendrier
  namespace: lexdata
spec:
  selector:
    app: lexdata-calendrier
  ports:
    - port: 9010
      targetPort: 9010
