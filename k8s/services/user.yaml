---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: lexdata-user
  namespace: lexdata
  labels:
    app: lexdata-user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lexdata-user
  template:
    metadata:
      labels:
        app: lexdata-user
    spec:
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: user
          image: lexdata/user-service:1.0.0
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          ports:
            - containerPort: 9003
          env:
            - name: SERVER_PORT
              value: "9003"
            - name: EUREKA_INSTANCE_HOSTNAME
              value: lexdata-user
          envFrom:
            - configMapRef:
                name: lexdata-config
            - secretRef:
                name: lexdata-secret
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 9003
            failureThreshold: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 9003
            initialDelaySeconds: 60
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 9003
            initialDelaySeconds: 40
            periodSeconds: 15
          resources:
            requests:
              memory: "384Mi"
              cpu: "200m"
            limits:
              memory: "768Mi"
              cpu: "750m"
---
apiVersion: v1
kind: Service
metadata:
  name: lexdata-user
  namespace: lexdata
spec:
  selector:
    app: lexdata-user
  ports:
    - port: 9003
      targetPort: 9003
