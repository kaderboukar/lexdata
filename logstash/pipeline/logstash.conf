input {
  tcp {
    port => 5044
    codec => json_lines
  }
}

output {
  elasticsearch {
    hosts => ["http://lexdata-elasticsearch:9200"]
    index => "lexdata-logs-%{+YYYY.MM.dd}"
  }
  stdout { codec => rubydebug }
}
